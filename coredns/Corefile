. {
    log
    debug
    template IN ANY google.com {
        match "^google\.com\.$"
        answer "google.com 60 IN CNAME app-1."
    }
    errors
    auto
    reload 2s
    forward . /etc/resolv.conf
}